{
  "stryker-config": {
    "project": "KEDA_Share.csproj",
    "reporters": [ "cleartexttree" ],
    "thresholds": {
      "high": 80,
      "low": 60,
      "break": 50
    },
    "mutate": [ "Repository/Implementations/*.cs", "Repository/Mongo/*.cs" ]
  }
}